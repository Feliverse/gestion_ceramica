<%= form_with(model: inventory_entry_detail) do |form| %>
  <% if inventory_entry_detail.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(inventory_entry_detail.errors.count, "error") %> prohibited this inventory_entry_detail from being saved:</h2>

      <ul>
        <% inventory_entry_detail.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :inventory_entry_id, style: "display: block" %>
    <%= form.text_field :inventory_entry_id %>
  </div>

  <div>
    <%= form.label :product_id, style: "display: block" %>
    <%= form.text_field :product_id %>
  </div>

  <div>
    <%= form.label :quantity, style: "display: block" %>
    <%= form.number_field :quantity %>
  </div>

  <div>
    <%= form.label :subtotal, style: "display: block" %>
    <%= form.text_field :subtotal %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
