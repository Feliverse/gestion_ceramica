<%= form_with(model: inventory_entry) do |form| %>
  <% if inventory_entry.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(inventory_entry.errors.count, "error") %> prohibited this inventory_entry from being saved:</h2>

      <ul>
        <% inventory_entry.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :warehouse_id, style: "display: block" %>
    <%= form.text_field :warehouse_id %>
  </div>

  <div>
    <%= form.label :date, style: "display: block" %>
    <%= form.datetime_field :date %>
  </div>

  <div>
    <%= form.label :total, style: "display: block" %>
    <%= form.text_field :total %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
